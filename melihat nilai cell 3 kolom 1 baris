from openpyxl import load_workbook

file = data.xlsx"
wb = load_workbook(file)
ws = wb.active
for c in range(1,4):
    print(ws.cell(row=2, column=c).value, end=' ') 
    #atau
    print(ws.cell(row=2, column=c).value)
